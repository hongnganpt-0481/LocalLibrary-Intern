extends ../layout

block content
  h1= t('titleListBookInstance')
  if bookInstances.length
    ul
      each bookInstance in bookInstances
        li
         a(href=`/bookInstances/${bookInstance.id}`) #{bookInstance.book.title} - #{bookInstance.imprint} (#{bookInstance.status})
  else
    p = t('noBookInstance')
